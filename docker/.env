# nautilus-managed defaults for docker compose.
# Step 5.5 (image conflict pre-check) may rewrite IMAGE to switch tag
# without overwriting <slug>:local. Edit IMAGE only; leave the marker line.
IMAGE=libero:local
